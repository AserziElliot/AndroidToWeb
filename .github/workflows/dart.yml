name: Compilar APK de Flutter

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Descargar codigo
      uses: actions/checkout@v4

    - name: Configurar Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Configurar Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'

    - name: Reconstruir Proyecto y Compilar
      run: |
        flutter create --org com.TuElliotFav --project-name stream_app mi_app_temporal
        PERMISO_INTERNET='<uses-permission android:name="android.permission.INTERNET"/>'
        sed -i "/<manifest/a \    $PERMISO_INTERNET" mi_app_temporal/android/app/src/main/AndroidManifest.xml
        cp lib/main.dart mi_app_temporal/lib/main.dart
        cd mi_app_temporal
        flutter pub add flutter_webrtc web_socket_channel
        flutter pub get
        flutter pub add flutter_webrtc web_socket_channel permission_handler
        flutter build apk --release
        cp build/app/outputs/flutter-apk/app-release.apk ../app-release.apk

    - name: Subir APK terminada
      uses: actions/upload-artifact@v4
      with:
        name: app-release
        path: app-release.apk
